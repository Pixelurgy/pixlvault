
name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Remove unused tools
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: false

      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .[test,dev]
      - name: Check formatting with ruff
        run: |
          ruff check pixlvault
      - name: Run tests serially in separate processes to limit resource usage
        run: |
          rc=0
          for test in tests/test_*.py; do
            echo "Running $test"
            python -m pytest -s -vvv --fast-captions --force-cpu --log-cli-level=INFO "$test" || rc=1
          done
          exit $rc
